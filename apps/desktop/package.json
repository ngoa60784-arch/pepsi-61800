{
    "name": "@tch/desktop",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "tauri": "tauri",
        "dev": "tauri dev",
        "build": "tauri build",
        "prepare-sidecar": "bun run ../../scripts/prepare-desktop-sidecar.ts",
        "prepare-sidecar:dev": "bun run ../../scripts/prepare-desktop-sidecar.ts --dev"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.4.0"
    }
}
